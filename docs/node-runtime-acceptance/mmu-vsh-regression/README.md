Default 128 MiB IMAC legacy-shell image: the existing mmu case passes. The existing vsh case passes after correcting exactly two stale golden lines for behavior already present in HEAD: Ctrl-C echo and vtop help. The initial failed diff is retained, as are both successful runner outputs and the frozen kernel hash. No runtime shell behavior was changed to pass this check. This is not the complete collection of separate MMU fault-injection cases.
