[package]
name = "vibeos-netstack"
version = "0.1.0"
edition = "2021"

[features]
default = ["tcp-echo"]
tcp-echo = []
net-shell = []
tcp-echo-recovery-test = ["tcp-echo"]

[dependencies]
vibeos-core = { path = "../../core" }
vibeos-net-protocol = { path = "../net-protocol" }
vibeos-vsh = { path = "../vsh", default-features = false }
