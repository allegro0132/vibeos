[package]
name = "vibeos-vsh"
version = "0.1.0"
edition = "2021"

[features]
default = ["pci-usb-help"]
pci-usb-help = []
network = []

[dependencies]
vibeos-core = { path = "../../core" }
vibeos-component-host = { path = "../../services/component-host" }
