Disabling Leap Tiering exposed a real pinned V8 RISC-V compile failure: OptimizeCodeOrTailCallOptimizedCodeSlot referenced a scratch scope outside its lifetime. Patch 0026 creates a scope at the tail-call site, includes t0/t1 and excludes live inputs/call registers before acquiring two temporaries. The exact previously failing RV64 target object now compiles, and patch application dry-runs with zero fuzz against the fresh prepared tree. Build 9 was deliberately interrupted to correct an initially invalid four-argument Exclude call after inspecting the two-register API; build 10 subsequently completed the full engine/snapshot build successfully in 623 seconds, including regeneration of embedded RV64 builtins. Configuration checks passed. No runtime acceptance follows from compilation; see engine-build-10.json and the final build-log excerpt.
