[package]
name = "vibeos-net-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
smoltcp = { version = "=0.13.1", default-features = false, features = [
    "alloc",
    "medium-ethernet",
    "proto-ipv4",
    "socket-dhcpv4",
    "socket-tcp",
    "socket-tcp-reno",
] }
vibeos-core = { path = "../../core" }
