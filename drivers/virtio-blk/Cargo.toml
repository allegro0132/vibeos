[package]
name = "vibeos-driver-virtio-blk"
version = "0.1.0"
edition = "2021"

[dependencies]
vibeos-driver-virtio-core = { path = "../virtio-core" }
vibeos-driver-virtio-mmio = { path = "../virtio-mmio" }
