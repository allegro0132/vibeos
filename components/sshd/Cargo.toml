[package]
name = "vibeos-sshd"
version = "0.1.0"
edition = "2021"

[features]
default = []

[dependencies]
vibeos-core = { path = "../../core" }
vibeos-ssh-identity = { path = "../../services/ssh-identity" }
sunset = { path = "../../vendor/sunset", default-features = false, features = ["alloc"] }
