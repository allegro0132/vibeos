[package]
name = "vibeos-sshd"
version = "0.1.0"
edition = "2021"

[features]
default = []
qualification-stream = []
# Default-off ownership seam for one exact authenticated SSH Component request
# parent. This exposes no profile publication or interpretation policy.
c84-profile-request-parent = []
# Default-off diagnostic phase sidecar for the exact managed Component request
# parent. This adds only non-consuming Host/Wait observations to the ownership
# seam; it does not finish, verify, stream, or publish a profile.
c84-profile-phase-sidecar = ["c84-profile-request-parent"]
# Target-only coordination for the C5.4c managed-native revoke gate. The
# production/Milk-V Platform surface remains default-open when this is absent.
native-revoke-target-acceptance = []

[dependencies]
vibeos-core = { path = "../../core" }
vibeos-component-host = { path = "../../services/component-host" }
vibeos-component-runtime = { path = "../../component-runtime" }
vibeos-net-api = { path = "../net-api" }
vibeos-net-protocol = { path = "../net-protocol" }
vibeos-random = { path = "../../random" }
vibeos-ssh-identity = { path = "../../services/ssh-identity" }
vibeos-vsh = { path = "../vsh", default-features = false }
sunset = { path = "../../vendor/sunset", default-features = false, features = ["alloc"] }

[dev-dependencies]
vibeos-component-admission = { path = "../../services/component-admission" }
vibeos-component-command = { path = "../../services/component-command" }
vibeos-image-policy = { path = "../../policy/image" }
