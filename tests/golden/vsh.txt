VibeOS shell ready -- type `help` for commands, `quiet` to mute components.
vibe> quiet
  background components muted; they keep running (`ps` proves it)
vibe> echo hello | wc > @console
1 1 6
vibe> spin
  vsh job %2: Cancelled
vibe> vsh if false; then echo hidden > @console; else echo branch > @console; fi
branch
vibe> vsh function greet who { echo "hello $who" > @console; }
vibe> vsh greet VibeOS
hello VibeOS
vibe> vsh echo "sub=$(echo inner)" > @console
sub=inner
vibe> vsh while false; do echo hidden > @console; done
vibe> echo history > @console
history
vibe> draft
vibe> echo history > @console
history
vibe> echo hllo
vibe> echo hello
hello
vibe> draft
vibe> echo hello
vibe> echo history > @console
vibe> echo hello
hello
vibe> vsh
VibeOS vsh diagnostic session -- Ctrl-C returns to `vibe>`.
vsh> help
  echo ...        write value arguments
  wc              count stdin bytes, words, and lines
  let NAME VALUE  set a session value
  if/while        bounded control flow (`; then` / `; do`)
  function N ...  define a value-only scoped function
  echo "$(...)"  bounded command substitution
  run-script @S   run a read-only manifested script
  jobs            list session jobs
  wait %N         join a job
  cancel %N       cancel a job
  ps              component lifecycle snapshots
  caps [space]    sanitized capability summary
  mem             bounded-memory accounts
  quiet           mute background component output
  verbose         restore background component output
  poweroff        power off
vsh> echo nested > @console
nested
vsh> ^C
  returning to diagnostic shell
vibe> 
