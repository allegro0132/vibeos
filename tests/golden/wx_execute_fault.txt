VibeOS shell ready -- type `help` for commands, `quiet` to mute components.
vibe> quiet
  background components muted; they keep running (`ps` proves it)
vibe> mmu wx fault execute
  W^X probe: execute writable 0xADDR
[!] fatal trap: cause=12 stval=0xADDR sepc=0xADDR (instruction page fault)
[!] W^X code pool blocked instruction page fault
