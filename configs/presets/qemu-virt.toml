# VibeOS build configuration. Edit this file or run ./configure.sh.
version = 1
boards = ["qemu-virt"]
components = [
    "file-tree",
    "network",
    "vsh",
]

[drivers]
virtio-blk = "on"
virtio-net = "on"
