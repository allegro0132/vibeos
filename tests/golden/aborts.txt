VibeOS shell ready -- type `help` for commands, `quiet` to mute components.
vibe> quiet
  background components muted; they keep running (`ps` proves it)
vibe> rustc edit
  enter your program; finish with a single `.` on its own line
  | fn main() -> i64 { let z = 0; 1 / z }
  | .
  compiled 1 fn -> 328 B of RV64 + 1 B of data in N us
  --- running natively ---
  --- aborted: attempt to divide by zero (after N us) ---
vibe> rustc edit
  enter your program; finish with a single `.` on its own line
  | fn main() -> i64 { 9223372036854775807 + 1 }
  | .
  error: line 1: this arithmetic operation will overflow: `9223372036854775807 + 1`
vibe> rustc edit
  enter your program; finish with a single `.` on its own line
  | fn main() -> i64 { let a = 9223372036854775807; let b = 1; a + b }
  | .
  compiled 1 fn -> 328 B of RV64 + 1 B of data in N us
  --- running natively ---
  --- aborted: attempt to perform arithmetic that overflowed (after N us) ---
vibe> rustc edit
  enter your program; finish with a single `.` on its own line
  | fn f(n: i64) -> i64 { f(n) }
  | fn main() -> i64 { f(0) }
  | .
  compiled 2 fn -> 396 B of RV64 + 1 B of data in N us
  --- running natively ---
  --- aborted: stack overflow (after N us) ---
vibe> rustc edit
  enter your program; finish with a single `.` on its own line
  | fn main() -> i64 { let mut i = 0; while i >= 0 { i = i + 0; } i }
  | .
  compiled 1 fn -> 464 B of RV64 + 1 B of data in N us
  --- running natively ---
  --- aborted: exceeded execution budget (after N us) ---
vibe> rustc hello
  compiled 1 fn -> 460 B of RV64 + 14 B of data in N us
  --- running natively ---
Hello, world!
  --- exited with 0 in N us ---
vibe> ps
  COMPONENT    TASK     NAME       CSPACE   STATE     REASON       POLLS     BUDGET
  component:1  task:1   sensor     sensor   running   -                9    65536 B
  component:2  task:2   logger     logger   running   -                9    65536 B
  component:3  task:3   guest      guest    running   -                3    65536 B
  component:4  task:4   shell      init     running   -               20   262144 B
  executor totals (including untracked tasks): 0 exited, 0 faulted
  memory budgets are declared; allocator accounting lands in 3.11
  background output is muted; poll counts still rising
vibe> 
