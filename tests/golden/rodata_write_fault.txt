VibeOS shell ready -- type `help` for commands, `quiet` to mute components.
vibe> quiet
  background components muted; they keep running (`ps` proves it)
vibe> mmu ro fault rodata
  read-only probe: write rodata 0xADDR
[!] fatal trap: cause=15 stval=0xADDR sepc=0xADDR (store page fault)
[!] read-only .rodata blocked store page fault
